class Solution(object):
    def orderlyQueue(self, s, k):
        if k == 1:
            res = s
            for i in range(1, len(s)):
                rotated = s[i:] + s[:i]
                res = min(res, rotated)
            return res
        else:
            return ''.join(sorted(s))
