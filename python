# The print() function outputs text to the screen
print("Hello, World!")

# The input() function captures what the user types
user_name = input("What is your name? ")

# Combine strings to create a personalized greeting
print(f"Welcome to coding in Python, {user_name}!")
